apiVersion: kpack.io/v1alpha2
kind: ClusterStack
metadata:
  name: jammy-base
spec:
  buildImage:
    image: paketobuildpacks/build-jammy-base:0.1.106
  id: io.buildpacks.stacks.jammy
  runImage:
    image: paketobuildpacks/run-jammy-base:0.1.106
